[project]
name = ""
version = "0.0.1"
requires-python = "~=3.12"
dependencies = [
    "ansible",
    "passlib",
]

[dependency-groups]
dev = ["ipykernel"]

[tool.uv]
package = false

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple"

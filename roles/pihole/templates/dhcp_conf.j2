#jinja2: trim_blocks:False
# Generated by Ansible
#
{% for host, properties in static_dhcp.items() -%}
dhcp-host={{properties.mac}},{{properties.ip|default(ip_prefix ~ properties.ip_suffix if properties.ip_suffix is defined)}},{{properties.hostname|default(host)}}{% if properties.nopihole is defined and properties.nopihole %},set:nopihole{% endif %}
{% endfor %}

dhcp-option=option:dns-server,{{nameserver_pi_ip}},{{homelab_nuc_ip}},8.8.8.8
dhcp-option=tag:nopihole,option:dns-server,{{router_ip}}
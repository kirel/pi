# Generated by Ansible
#
{% for host, properties in static_dhcp.items() %}
dhcp-host={{properties.mac}},{{properties.ip|default(ip_prefix ~ properties.ip_suffix if properties.ip_suffix is defined)}},{{properties.hostname|default(host)}}
{% endfor %}
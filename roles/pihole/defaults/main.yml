---
# Default DNS servers to assign to clients tagged with 'nopihole'
google_dns_servers:
  - 8.8.8.8
  - 8.8.4.4

# Pi-hole cache optimization settings
pihole_cache_size: 10000
pihole_rate_limit_count: 10000
pihole_rate_limit_interval: 60

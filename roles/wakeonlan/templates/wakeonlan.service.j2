[Unit]
Description=Enable Wake On LAN for {{ wakeonlan_interface }}
After=network.target

[Service]
Type=oneshot
ExecStart=/usr/sbin/ethtool -s {{ wakeonlan_interface }} wol g

[Install]
WantedBy=multi-user.target

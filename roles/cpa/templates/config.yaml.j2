---
host: ""
port: 8317

tls:
  enable: false
  cert: ""
  key: ""

remote-management:
  # Usage Keeper runs in a separate container and needs the Management API.
  allow-remote: true
  secret-key: "{{ cpa_management_key }}"
  disable-control-panel: false

auth-dir: "~/.cli-proxy-api"

api-keys:
  - "{{ cpa_api_key }}"

# TEMPORARY: retain the Codex request/response conversion for the gpt-5.6
# tool-calling investigation. Disable both settings after the A/B run.
debug: true
request-log: true
logging-to-file: true
logs-max-total-size-mb: 256
usage-statistics-enabled: true
redis-usage-queue-retention-seconds: 3600

request-retry: 3
max-retry-credentials: 0
max-retry-interval: 30

routing:
  strategy: round-robin
  session-affinity: false
